<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %></title>
    <link rel="stylesheet" href="/styles/blog.css">
</head>
<body>
    <div class="blog-container">
       
        <div class="banner">
            <img src="<%= blog.bannerImage || '/default-banner.jpg' %>" alt="Blog Banner" class="banner-image">
            <div class="overlay"></div>
            <div class="banner-text">
                <h1 class="blog-title"><%= blog.title %></h1>
                <p class="blog-date">Published on <%= blog.publishedAt || new Date().toLocaleDateString() %></p>
            </div>
        </div>

       
        <div class="blog-content">
            <%= (blog.content || '').replace(/\n/g, "<br>") %>
        </div>

        
        <div class="blog-actions">
            <a href="/edit/<%= blog.id %>" class="btn edit-btn">Edit</a>
            <form action="/delete/<%= blog.id %>" method="POST" class="delete-form">
                <button type="submit" class="btn delete-btn">Delete</button>
            </form>
        </div>

        <a href="/" class="btn back-btn">← Back to Home</a>
    </div>
    
</body>
</html>
